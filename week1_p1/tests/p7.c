extern void print(int); 
extern int read();

int func (int i) {
    int a;
    int b;
    int result;

    a = i * 10;
    b = i * 2;
}
// This should result in a syntax error (miniC doesn't allow comments)